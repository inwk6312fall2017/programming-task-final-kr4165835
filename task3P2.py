file=open("running-config.cfg" ,'r')
Acess_file=file.readline() 
A=" "
For word in Acessfile :
  if word = "access": 
   A.appand(Acess_file,'/n') 
  else:
   pass..
dict={}
for line in A:
item=line.strip()
dict("item[2:4]"=item(len[item])  

print(dict)

